{
  "name": "@adonisjs/msgpack-encoder",
  "version": "1.0.0",
  "description": "Message pack encoder to encoder and decode packages",
  "main": "index.js",
  "browser": "browser.js",
  "module": "browser.js",
  "files": [
    "index.js",
    "browser.js"
  ],
  "scripts": {
    "build": "babel --out-file=browser.js index.js --presets=env",
    "lint": "standard",
    "prepublishOnly": "npm run build",
    "pretest": "npm run lint",
    "test": "nyc japa",
    "posttest": "nyc report --reporter=text-lcov | coveralls"
  },
  "keywords": [
    "websocket",
    "msgpack",
    "msgpack-lite"
  ],
  "author": "virk",
  "license": "MIT",
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-core": "^6.26.0",
    "babel-preset-env": "^1.6.1",
    "coveralls": "^3.0.0",
    "cz-conventional-changelog": "^2.1.0",
    "japa": "^1.0.6",
    "japa-cli": "^1.0.1",
    "nyc": "^11.4.1",
    "standard": "^11.0.0"
  },
  "dependencies": {
    "msgpack-lite": "^0.1.26"
  },
  "config": {
    "commitizen": {
      "path": "./node_modules/cz-conventional-changelog"
    }
  }
}
